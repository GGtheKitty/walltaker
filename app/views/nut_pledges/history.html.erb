<h2>NNN History</h2>

<% if @nut_pledges.empty? %>
  <p>No NNN pledges yet.</p>
<% else %>
  <table>
    <thead>
    <tr>
      <th>Year</th>
      <th>Status</th>
      <th>Signed</th>
    </tr>
    </thead>
    <tbody>
    <% @nut_pledges.each do |pledge| %>
      <tr>
        <td>NNN<%= pledge.year %></td>
        <td>
          <% if pledge.failed? %>
            <div class="nut-pledge-history__status">
              <span>Failed on day <%= pledge.failed_on.strftime("%e") %></span>
              <%= render 'nut_pledges/failure_preview', past_link: pledge.past_link, css_class: 'nut-pledge-history__preview' %>
            </div>
          <% elsif pledge.succeeded? %>
            Succeeded
          <% else %>
            In progress
          <% end %>
        </td>
        <td><%= pledge.created_at.strftime("%B %-d, %Y") %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
<% end %>
